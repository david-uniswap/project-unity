{
  "name": "@project-unity/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "pipeline:once": "bun run src/index.ts --once"
  },
  "dependencies": {
    "@openzeppelin/merkle-tree": "^1.0.7",
    "postgres": "^3.4.5",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "bun-types": "^1.1.0",
    "@types/node": "^20.0.0"
  }
}
